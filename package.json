{
  "name": "meteor_shop",
  "private": true,
  "scripts": {
    "start": "meteor run --settings settings.json --port 80"
  },
  "dependencies": {
    "babel-runtime": "^6.20.0",
    "meteor-node-stubs": "~0.2.4"
  }
}
